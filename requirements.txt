openai>=1.43.0
langchain-openai>=0.1.14
langgraph>=0.2.0
langgraph-checkpoint>=2.1.0,<3.0.0
langgraph-checkpoint-postgres>=2.0.0,<3.0.0
fastapi>=0.114.0
uvicorn[standard]>=0.30.0
python-dotenv>=1.0.1
pydantic>=2.7.0
psycopg[binary]>=3.2.1
psycopg-pool>=3.2.1
prometheus-fastapi-instrumentator>=7.0.0
mem0ai[graph]
qdrant-client>=1.8.2
neo4j>=5.20.0
llama-index>=0.10.60
pytest>=8.3.0
dashscope>=1.23.1
websockets>=12.0,<14.0  # 避免14.0版本的recv_messages兼容性问题
structlog>=24.1.0
python-multipart>=0.0.20
opuslib>=3.0.1
numpy>=1.24.0,<2.0.0

# PyTorch CPU-only版本（用于语音VAD检测，不需要GPU）
# 重要：必须使用CPU-only版本以避免WSL2环境中的Bus error
# 安装方法：pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
# 当前venv中已安装：torch==2.4.1+cpu, torchaudio==2.4.1+cpu
torch>=2.0.0
torchaudio>=2.0.0

httpx>=0.27.0
jsonschema>=4.19.0

# 视频分析依赖（用于视频流截取和GLM-4V视觉分析）
opencv-python>=4.8.0  # 视频流处理和帧截取
Pillow>=10.0.0        # 图像格式转换

# Redis 缓存
redis>=5.0.0

# Instructor - LLM结构化输出（用于Markdown侦察方案解析）
instructor>=1.10.0
