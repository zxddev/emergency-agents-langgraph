# Dev environment for AI Emergency Brain
# Server endpoints (replace with your own service addresses when deploying)
QDRANT_URL=
NEO4J_URI=
NEO4J_USER=
NEO4J_PASSWORD=
POSTGRES_DSN=

# OpenAI-compatible LLM (使用智谱AI GLM)
OPENAI_BASE_URL=
OPENAI_API_KEY=
LLM_MODEL=

# Embedding (使用智谱AI的embedding-3模型)
EMBEDDING_MODEL=
EMBEDDING_DIM=

# --- ASR (语音识别) 配置 ---
# 说明：本项目严格使用单一提供方，不做降级。请按需选择 aliyun 或 local。
# 1) 阿里云百炼（在线）
DASHSCOPE_API_KEY=  # required if ASR_PROVIDER=aliyun
ASR_PROVIDER=
# 阿里云百炼API Key（北京地域）
# 2) 本地 FunASR（私有化部署）
# 如使用本地ASR，请设置 ASR_PROVIDER=local，并配置以下URL（自签名HTTPS默认信任）
VOICE_ASR_WS_URL=
FUNASR_HOTWORDS_JSON=
FUNASR_CHUNK_SIZE=
