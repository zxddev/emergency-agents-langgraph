# Dev environment for AI Emergency Brain
# Server endpoints - Using remote server at 8.147.130.215
QDRANT_URL=http://8.147.130.215:6333
NEO4J_URI=bolt://8.147.130.215:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=example-neo4j
POSTGRES_DSN=postgresql://rescue:rescue_password@8.147.130.215:19532/rescue_system
CHECKPOINT_SQLITE_PATH=./temp/checkpoints.sqlite3

# LLM API - Using cloud provider API (e.g., ZhipuAI, OpenAI, etc.)
# Please replace with your actual API endpoint and key
OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
OPENAI_API_KEY=your_api_key_here
LLM_MODEL=glm-4-flash

# Embedding - Using cloud embedding service
EMBEDDING_MODEL=embedding-3
EMBEDDING_DIM=2048

# --- ASR (语音识别) 配置 ---
# 说明：本项目严格使用单一提供方，不做降级。请按需选择 aliyun 或 local。
# 1) 阿里云百炼（在线）
DASHSCOPE_API_KEY=
ASR_PROVIDER=local
# 阿里云百炼API Key（北京地域）
# 2) 本地 FunASR（私有化部署）
# 如使用本地ASR，请设置 ASR_PROVIDER=local，并配置以下URL（自签名HTTPS默认信任）
VOICE_ASR_WS_URL=
FUNASR_HOTWORDS_JSON=
FUNASR_CHUNK_SIZE=

# ASR 故障转移配置
ASR_PRIMARY_PROVIDER=aliyun
ASR_FALLBACK_PROVIDER=local
