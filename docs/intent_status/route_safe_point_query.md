# 意图：route_safe_point_query

## 标准槽位
- `lat`
- `lng`
- `policy`（默认 best）

## 当前实现
- 仅 Schema 定义，无实际处理逻辑。

## 待完成闭环
1. **规划能力确认**：决定调用高德/自研路径规划 API，明确 `policy` 枚举。
2. **实现 Handler**：输入坐标，输出路线与安全点列表，必要时整合避险规则。
3. **结果呈现**：返回路线 GeoJSON、ETA、推荐安全点等结构化数据。
4. **缓存与限流**：对高德接口使用缓存/速率控制，防止限额耗尽。

## 依赖与注意事项
- 需要同 `rescue-task-generate` 共享的高德客户端或新增能力。
- 注意与任务进度查询中的路线数据一致，避免重复字段设计。
