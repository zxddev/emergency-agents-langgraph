# 意图：plan_task_approval

## 标准槽位
- `target_type`
- `target_id`
- `decision`
- `reason`（可选）

## 当前实现
- 仅存在 Schema，未实现任何处理逻辑；router 也未注册。

## 待完成闭环
1. **流程设计**：明确审批通过/驳回后需要同步的任务、计划对象。
2. **实现 Handler**：落地审批结果（写库/调用 Java），并处理人工确认（可能需要 interrupt）。
3. **状态同步**：向前端/其他系统广播审批结论。
4. **审计记录**：把审批动作、意见、操作人写入日志与数据库。

## 依赖与注意事项
- 意图为高风险操作，建议沿用机器人狗的确认机制。
- 需与任务调度、计划编制团队对齐审批流程与权限模型。
