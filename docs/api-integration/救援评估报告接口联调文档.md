# 救援评估报告接口 - 联调文档

**接口版本**: v1.0
**最后更新**: 2025-11-02
**负责人**: AI应急大脑团队

---

## 1. 接口概述

### 1.1 功能说明

本接口用于**救灾结束后的事后总结评估**，基于完整的救援过程数据生成专业的评估报告。适用于救援工作全部完成后，由应急管理部门或评估专家组进行系统性复盘分析。

**核心能力**:
- ✅ 自动计算5个关键量化指标（响应及时性、救援成功率、资源效率等）
- ✅ 生成标准7章节评估报告（符合政府评估报告规范）
- ✅ 对比预案要求和历史案例，指出差距和进步
- ✅ 问题导向分析，给出具体可操作的改进建议
- ✅ 自动标注数据缺失字段并评估影响

**使用场景**:
- 📊 应急管理部门总结救援经验
- 📝 编写政府工作报告和白皮书
- 🔍 专家组事后评估和复盘
- 📈 救援能力评估和改进
- 📚 历史案例库建设

**与救灾方案上报接口的区别**:

| 对比项 | 救灾方案上报 | 救援评估报告 |
|--------|-------------|-------------|
| **时机** | 灾害发生时（实时） | 救援结束后（事后） |
| **目的** | 快速生成行动方案 | 系统性总结评估 |
| **输入** | 简单灾情描述 | 完整救援过程数据 |
| **输出** | 行动建议 + 装备推荐 | 7章专业评估报告 |
| **时效** | 紧急（分钟级） | 不紧急（天级） |

---

## 2. 接口信息

### 2.1 基本信息

| 项目 | 内容 |
|------|------|
| **接口地址** | `POST /reports/post-rescue-assessment` |
| **请求方式** | POST |
| **Content-Type** | application/json |
| **超时时间** | 建议180秒（完整输入可能需要120秒） |
| **鉴权方式** | (根据实际部署配置) |

### 2.2 环境地址

| 环境 | 地址 | 说明 |
|------|------|------|
| 开发环境 | `http://localhost:8008` | 本地开发 |
| 测试环境 | `http://test-server:8008` | 联调测试 |
| 生产环境 | `https://api.emergency.cn` | 正式环境 |

---

## 3. 请求参数

### 3.1 数据结构概览

请求体包含6个主要模块：

```
PostRescueAssessmentInput
├── disaster_overview        # 灾害概况（必填）
├── response_activation      # 响应启动（必填）
├── timeline                 # 时间线（可选但推荐）
├── forces_deployed          # 投入力量（可选但推荐）
├── rescue_statistics        # 救援统计（必填）
├── coordination             # 协同配合（可选）
├── challenges               # 遇到的挑战（可选）
└── resource_utilization     # 资源投入（可选）
```

### 3.2 详细字段说明

#### 3.2.1 disaster_overview（灾害概况）- 必填

```json
{
  "disaster_type": "string",           // 必填，灾害类型
  "disaster_name": "string",           // 必填，灾害名称
  "occurrence_time": "datetime",       // 必填，发生时间（ISO 8601格式）
  "end_time": "datetime",              // 必填，结束时间
  "location": "string",                // 必填，地点
  "disaster_scale": "string",          // 必填，灾害规模描述
  "affected_area_km2": "number",       // 可选，影响面积（平方公里）
  "final_deaths": "number",            // 必填，最终死亡人数
  "final_injured": "number",           // 必填，最终受伤人数
  "affected_population": "number",     // 必填，受影响人口
  "economic_loss_billion": "number"    // 可选，经济损失（亿元）
}
```

**示例**:
```json
{
  "disaster_type": "地震灾害",
  "disaster_name": "雅安7.0级地震",
  "occurrence_time": "2025-04-20T08:02:00",
  "end_time": "2025-05-05T18:00:00",
  "location": "四川省雅安市芦山县",
  "disaster_scale": "7.0级，震源深度13公里",
  "affected_area_km2": 12500.0,
  "final_deaths": 196,
  "final_injured": 11826,
  "affected_population": 2308000,
  "economic_loss_billion": 86.8
}
```

#### 3.2.2 response_activation（响应启动）- 必填

```json
{
  "response_level": "string",          // 必填，响应级别（枚举）
  "activation_time": "datetime",       // 必填，启动时间
  "command_center": "string",          // 必填，指挥机构名称
  "leading_unit": "string",            // 必填，牵头单位
  "participating_departments": ["string"], // 必填，参与部门列表
  "legal_basis": "string"              // 可选，法律依据
}
```

**response_level 枚举值**:
- `一级响应`: 特别重大灾害
- `二级响应`: 重大灾害
- `三级响应`: 较大灾害
- `四级响应`: 一般灾害

**示例**:
```json
{
  "response_level": "一级响应",
  "activation_time": "2025-04-20T08:35:00",
  "command_center": "国务院抗震救灾指挥部",
  "leading_unit": "应急管理部",
  "participating_departments": [
    "应急管理部",
    "国家消防救援局",
    "中央军委联合参谋部",
    "国家卫生健康委",
    "交通运输部"
  ],
  "legal_basis": "《国家地震应急预案》（2024年修订）"
}
```

#### 3.2.3 timeline（时间线）- 可选但推荐

记录关键事件节点，帮助评估响应及时性。

```json
[
  {
    "time": "datetime",              // 必填，事件时间
    "event_type": "string",          // 必填，事件类型
    "description": "string",         // 必填，事件描述
    "significance": "string"         // 可选，重要性说明
  }
]
```

**event_type 建议值**:
- `灾害发生`
- `响应启动`
- `首支队伍到达`
- `国家力量集结`
- `黄金72小时救援`
- `搜救阶段结束`
- `响应终止`

**示例**:
```json
[
  {
    "time": "2025-04-20T08:02:00",
    "event_type": "灾害发生",
    "description": "雅安市芦山县发生7.0级地震",
    "significance": "震中位于人口密集区，灾情严重"
  },
  {
    "time": "2025-04-20T08:35:00",
    "event_type": "响应启动",
    "description": "国务院启动一级应急响应",
    "significance": "33分钟内完成响应启动决策"
  },
  {
    "time": "2025-04-20T09:20:00",
    "event_type": "首支队伍到达",
    "description": "四川消防救援总队首支力量到达震中",
    "significance": "距地震发生78分钟"
  }
]
```

#### 3.2.4 forces_deployed（投入力量）- 可选但推荐

```json
[
  {
    "force_type": "string",           // 必填，力量类型
    "force_name": "string",           // 必填，单位名称
    "personnel": "number",            // 必填，人员数量
    "arrival_time": "datetime",       // 可选，到达时间
    "main_equipment": "string",       // 可选，主要装备
    "assigned_tasks": "string",       // 可选，承担任务
    "rescued_people": "number",       // 可选，救援人数
    "completed_tasks": "string"       // 可选，完成任务描述
  }
]
```

**force_type 建议值**:
- `国家综合性消防救援队伍`
- `解放军和武警部队`
- `医疗救援队伍`
- `社会救援力量`
- `志愿者队伍`

**示例**:
```json
[
  {
    "force_type": "国家综合性消防救援队伍",
    "force_name": "国家地震灾害紧急救援队",
    "personnel": 480,
    "arrival_time": "2025-04-20T11:30:00",
    "main_equipment": "生命探测仪12台、破拆工具组50套、医疗急救设备15套、搜救犬20只",
    "assigned_tasks": "震中核心区域重点建筑搜救",
    "rescued_people": 127,
    "completed_tasks": "成功搜救127人，其中生还112人，处置危险建筑38栋"
  }
]
```

#### 3.2.5 rescue_statistics（救援统计）- 必填

```json
{
  "total_rescued": "number",           // 必填，累计搜救人数
  "rescued_alive": "number",           // 必填，救出生还者
  "evacuated_people": "number",        // 必填，转移人数
  "resettled_people": "number",        // 必填，安置人数
  "treated_patients": "number",        // 必填，救治人数
  "transferred_critical": "number",    // 可选，转运重症人数
  "relief_materials_distributed": "string", // 可选，物资发放描述
  "roads_reopened_km": "number",       // 可选，抢通道路（公里）
  "power_restored_households": "number", // 可选，恢复供电户数
  "water_restored_households": "number"  // 可选，恢复供水户数
}
```

**示例**:
```json
{
  "total_rescued": 782,
  "rescued_alive": 698,
  "evacuated_people": 230800,
  "resettled_people": 156000,
  "treated_patients": 11826,
  "transferred_critical": 238,
  "relief_materials_distributed": "帐篷5.2万顶、棉被15万床、食品1200吨、饮用水800吨",
  "roads_reopened_km": 215.0,
  "power_restored_households": 45000,
  "water_restored_households": 38000
}
```

#### 3.2.6 coordination（协同配合）- 可选

```json
{
  "government_agencies": "string",     // 政府部门协同情况
  "military_cooperation": "string",    // 军地协同情况
  "social_forces": "string",           // 社会力量协同情况
  "inter_regional": "string",          // 跨区域协同情况
  "information_sharing": "string",     // 信息共享情况
  "coordination_effectiveness": "string" // 协同效果评价
}
```

#### 3.2.7 challenges（遇到的挑战）- 可选

```json
{
  "access_difficulties": "string",      // 交通阻碍
  "communication_issues": "string",     // 通信问题
  "equipment_shortages": "string",      // 装备短缺
  "personnel_issues": "string",         // 人员问题
  "weather_impact": "string",           // 天气影响
  "secondary_disasters": "string",      // 次生灾害
  "coordination_problems": "string",    // 协调问题
  "other_challenges": "string"          // 其他挑战
}
```

#### 3.2.8 resource_utilization（资源投入）- 可选

```json
{
  "total_personnel": "number",          // 总投入人员
  "total_vehicles": "number",           // 总投入车辆
  "aircraft_sorties": "number",         // 飞机架次
  "relief_tents": "number",             // 救援帐篷数
  "relief_quilts": "number",            // 棉被数
  "food_tons": "number",                // 食品吨数
  "water_tons": "number",               // 饮用水吨数
  "total_funds_million": "number",      // 总资金（百万元）
  "central_funds_million": "number",    // 中央资金
  "local_funds_million": "number"       // 地方资金
}
```

### 3.3 最小请求示例

```json
{
  "disaster_overview": {
    "disaster_type": "地震灾害",
    "disaster_name": "简化测试地震",
    "occurrence_time": "2025-01-02T14:28:00",
    "end_time": "2025-01-10T18:00:00",
    "location": "四川省",
    "disaster_scale": "6.0级",
    "final_deaths": 50,
    "final_injured": 200,
    "affected_population": 10000
  },
  "response_activation": {
    "response_level": "三级响应",
    "activation_time": "2025-01-02T15:00:00",
    "command_center": "省应急指挥中心",
    "leading_unit": "省应急管理厅",
    "participating_departments": ["消防救援", "卫生健康", "交通运输"]
  },
  "rescue_statistics": {
    "total_rescued": 100,
    "rescued_alive": 80,
    "evacuated_people": 5000,
    "resettled_people": 3000,
    "treated_patients": 150
  },
  "resource_utilization": {
    "total_personnel": 500
  }
}
```

---

## 4. 响应参数

### 4.1 成功响应（HTTP 200）

```json
{
  "report_text": "string",            // Markdown格式的完整报告（4000-6000字符）
  "key_metrics": {
    "response_time_seconds": "number",    // 响应时间（秒）
    "response_time_hours": "number",      // 响应时间（小时）
    "rescue_duration_days": "number",     // 救援持续天数
    "rescue_success_rate": "number",      // 救援成功率（0-1）
    "per_capita_rescued": "number",       // 人均救援人数
    "completeness": "number"              // 数据完整度（0-1）
  },
  "data_sources": ["string"],         // 数据来源列表
  "confidence_score": "number",       // 置信度（0-1）
  "referenced_specs": ["string"],     // 参考规范列表
  "referenced_cases": ["string"],     // 参考案例列表
  "errors": ["string"]                // 错误信息（如有）
}
```

### 4.2 字段详细说明

#### report_text（评估报告）

**格式**: Markdown文本，标准7章结构

**章节结构**:
```
# 地震救援工作总结评估报告

## 一、灾害基本情况
灾害名称、时间、地点、规模、伤亡、经济损失

## 二、应急响应过程
响应级别、启动时间、指挥体系、响应及时性分析、对比预案要求

## 三、救援力量投入与调度
力量清单、规模评估、到位及时性、任务分工、装备配置

## 四、救援成效评估
核心数据、量化指标（成功率、效率）、与目标对比

## 五、协同配合与信息共享
政府协同、军地协同、社会力量、跨区域协同、信息共享效率

## 六、存在的问题与不足
问题列表、根本原因分析、量化影响、责任归属

## 七、经验总结与改进建议
### 7.1 成功经验与可复制做法
### 7.2 改进建议（具体、可操作）
### 7.3 需要上级部门协调的事项
```

#### key_metrics（关键指标）

**自动计算的5个核心指标**:

| 指标 | 说明 | 计算公式 | 示例值 |
|------|------|---------|--------|
| response_time_seconds | 响应启动时间（秒） | 启动时间 - 发生时间 | 1920 |
| response_time_hours | 响应启动时间（小时） | response_time_seconds / 3600 | 0.53 |
| rescue_duration_days | 救援持续天数 | 结束时间 - 发生时间 | 8.15 |
| rescue_success_rate | 救援成功率 | 生还人数 / 搜救总数 | 0.8 |
| per_capita_rescued | 人均救援人数 | 搜救总数 / 投入人员 | 0.2 |
| completeness | 数据完整度 | 非空字段数 / 总字段数 | 0.65 |

#### confidence_score（置信度）

**计算规则**:
```
置信度 = (数据完整度 × 0.4) +
        (时间戳质量 × 0.2) +
        (统计数据质量 × 0.2) +
        (参考资料数量 × 0.2)
```

| 分数范围 | 含义 | 报告质量 |
|---------|------|---------|
| 0.8-1.0 | 高置信度 | 可作为正式报告 |
| 0.6-0.79 | 中等置信度 | 需人工审核完善 |
| 0-0.59 | 低置信度 | 需补充大量数据 |

---

## 5. 完整调用示例

### 5.1 cURL命令（最小输入）

```bash
curl -X POST http://localhost:8008/reports/post-rescue-assessment \
  -H "Content-Type: application/json" \
  --max-time 180 \
  -d '{
    "disaster_overview": {
      "disaster_type": "地震灾害",
      "disaster_name": "简化测试地震",
      "occurrence_time": "2025-01-02T14:28:00",
      "end_time": "2025-01-10T18:00:00",
      "location": "四川省",
      "disaster_scale": "6.0级",
      "final_deaths": 50,
      "final_injured": 200,
      "affected_population": 10000
    },
    "response_activation": {
      "response_level": "三级响应",
      "activation_time": "2025-01-02T15:00:00",
      "command_center": "省应急指挥中心",
      "leading_unit": "省应急管理厅",
      "participating_departments": ["消防救援", "卫生健康", "交通运输"]
    },
    "rescue_statistics": {
      "total_rescued": 100,
      "rescued_alive": 80,
      "evacuated_people": 5000,
      "resettled_people": 3000,
      "treated_patients": 150
    },
    "resource_utilization": {
      "total_personnel": 500
    }
  }'
```

### 5.2 成功响应示例

```json
{
  "report_text": "# 地震救援工作总结评估报告\n\n## 一、灾害基本情况\n\n...(完整的4000-6000字Markdown报告)",
  "key_metrics": {
    "response_time_seconds": 1920.0,
    "response_time_hours": 0.53,
    "rescue_duration_days": 8.15,
    "rescue_success_rate": 0.8,
    "per_capita_rescued": 0.2,
    "completeness": 0.25
  },
  "data_sources": [
    "RAG规范库"
  ],
  "confidence_score": 0.208,
  "referenced_specs": [],
  "referenced_cases": [],
  "errors": []
}
```

---

## 6. 前端集成示例

### 6.1 Vue 3 + Element Plus

```vue
<template>
  <div>
    <el-steps :active="currentStep" finish-status="success">
      <el-step title="输入数据" />
      <el-step title="生成报告" />
      <el-step title="查看结果" />
    </el-steps>

    <!-- 步骤1：输入表单 -->
    <el-form v-if="currentStep === 0" :model="form" ref="formRef">
      <el-card header="灾害概况">
        <el-form-item label="灾害名称" prop="disaster_overview.disaster_name" required>
          <el-input v-model="form.disaster_overview.disaster_name" />
        </el-form-item>

        <el-form-item label="灾害类型" prop="disaster_overview.disaster_type" required>
          <el-select v-model="form.disaster_overview.disaster_type">
            <el-option label="地震灾害" value="地震灾害" />
            <el-option label="洪涝灾害" value="洪涝灾害" />
            <el-option label="山体滑坡" value="山体滑坡" />
          </el-select>
        </el-form-item>

        <el-form-item label="发生时间" prop="disaster_overview.occurrence_time" required>
          <el-date-picker
            v-model="form.disaster_overview.occurrence_time"
            type="datetime"
            format="YYYY-MM-DD HH:mm:ss"
          />
        </el-form-item>

        <el-form-item label="结束时间" prop="disaster_overview.end_time" required>
          <el-date-picker
            v-model="form.disaster_overview.end_time"
            type="datetime"
            format="YYYY-MM-DD HH:mm:ss"
          />
        </el-form-item>

        <el-form-item label="地点" prop="disaster_overview.location" required>
          <el-input v-model="form.disaster_overview.location" />
        </el-form-item>

        <el-form-item label="灾害规模" prop="disaster_overview.disaster_scale" required>
          <el-input v-model="form.disaster_overview.disaster_scale" />
        </el-form-item>

        <el-row :gutter="20">
          <el-col :span="8">
            <el-form-item label="死亡人数" prop="disaster_overview.final_deaths" required>
              <el-input-number v-model="form.disaster_overview.final_deaths" :min="0" />
            </el-form-item>
          </el-col>
          <el-col :span="8">
            <el-form-item label="受伤人数" prop="disaster_overview.final_injured" required>
              <el-input-number v-model="form.disaster_overview.final_injured" :min="0" />
            </el-form-item>
          </el-col>
          <el-col :span="8">
            <el-form-item label="受影响人口" prop="disaster_overview.affected_population" required>
              <el-input-number v-model="form.disaster_overview.affected_population" :min="0" />
            </el-form-item>
          </el-col>
        </el-row>
      </el-card>

      <el-card header="响应启动" style="margin-top: 20px">
        <el-form-item label="响应级别" prop="response_activation.response_level" required>
          <el-radio-group v-model="form.response_activation.response_level">
            <el-radio label="一级响应" />
            <el-radio label="二级响应" />
            <el-radio label="三级响应" />
            <el-radio label="四级响应" />
          </el-radio-group>
        </el-form-item>

        <el-form-item label="启动时间" prop="response_activation.activation_time" required>
          <el-date-picker
            v-model="form.response_activation.activation_time"
            type="datetime"
            format="YYYY-MM-DD HH:mm:ss"
          />
        </el-form-item>

        <el-form-item label="指挥机构" prop="response_activation.command_center" required>
          <el-input v-model="form.response_activation.command_center" />
        </el-form-item>

        <el-form-item label="牵头单位" prop="response_activation.leading_unit" required>
          <el-input v-model="form.response_activation.leading_unit" />
        </el-form-item>

        <el-form-item label="参与部门" prop="response_activation.participating_departments" required>
          <el-select
            v-model="form.response_activation.participating_departments"
            multiple
            placeholder="选择参与部门"
          >
            <el-option label="应急管理部" value="应急管理部" />
            <el-option label="国家消防救援局" value="国家消防救援局" />
            <el-option label="国家卫生健康委" value="国家卫生健康委" />
            <el-option label="交通运输部" value="交通运输部" />
          </el-select>
        </el-form-item>
      </el-card>

      <el-card header="救援统计" style="margin-top: 20px">
        <el-row :gutter="20">
          <el-col :span="12">
            <el-form-item label="累计搜救人数" prop="rescue_statistics.total_rescued" required>
              <el-input-number v-model="form.rescue_statistics.total_rescued" :min="0" />
            </el-form-item>
          </el-col>
          <el-col :span="12">
            <el-form-item label="救出生还者" prop="rescue_statistics.rescued_alive" required>
              <el-input-number v-model="form.rescue_statistics.rescued_alive" :min="0" />
            </el-form-item>
          </el-col>
        </el-row>

        <el-row :gutter="20">
          <el-col :span="8">
            <el-form-item label="转移人数" prop="rescue_statistics.evacuated_people" required>
              <el-input-number v-model="form.rescue_statistics.evacuated_people" :min="0" />
            </el-form-item>
          </el-col>
          <el-col :span="8">
            <el-form-item label="安置人数" prop="rescue_statistics.resettled_people" required>
              <el-input-number v-model="form.rescue_statistics.resettled_people" :min="0" />
            </el-form-item>
          </el-col>
          <el-col :span="8">
            <el-form-item label="救治人数" prop="rescue_statistics.treated_patients" required>
              <el-input-number v-model="form.rescue_statistics.treated_patients" :min="0" />
            </el-form-item>
          </el-col>
        </el-row>
      </el-card>

      <el-card header="资源投入" style="margin-top: 20px">
        <el-form-item label="总投入人员" prop="resource_utilization.total_personnel">
          <el-input-number v-model="form.resource_utilization.total_personnel" :min="0" />
        </el-form-item>
      </el-card>

      <el-button type="primary" @click="nextStep" style="margin-top: 20px">
        下一步：生成报告
      </el-button>
    </el-form>

    <!-- 步骤2：生成中 -->
    <div v-if="currentStep === 1" class="generating">
      <el-progress
        :percentage="progress"
        :status="progress === 100 ? 'success' : undefined"
      />
      <p>{{ progressText }}</p>
    </div>

    <!-- 步骤3：查看结果 -->
    <div v-if="currentStep === 2 && result">
      <el-card>
        <template #header>
          <div style="display: flex; justify-content: space-between; align-items: center;">
            <span>评估报告</span>
            <div>
              <el-tag :type="getConfidenceType(result.confidence_score)" size="large">
                置信度: {{ (result.confidence_score * 100).toFixed(0) }}%
              </el-tag>
              <el-button
                type="primary"
                @click="downloadReport"
                style="margin-left: 10px"
              >
                下载报告
              </el-button>
            </div>
          </div>
        </template>

        <!-- 关键指标 -->
        <el-descriptions title="关键指标" :column="3" border>
          <el-descriptions-item label="响应时间">
            {{ result.key_metrics.response_time_hours.toFixed(2) }} 小时
          </el-descriptions-item>
          <el-descriptions-item label="救援持续">
            {{ result.key_metrics.rescue_duration_days.toFixed(1) }} 天
          </el-descriptions-item>
          <el-descriptions-item label="救援成功率">
            {{ (result.key_metrics.rescue_success_rate * 100).toFixed(1) }}%
          </el-descriptions-item>
          <el-descriptions-item label="人均救援">
            {{ result.key_metrics.per_capita_rescued.toFixed(2) }} 人/人
          </el-descriptions-item>
          <el-descriptions-item label="数据完整度">
            {{ (result.key_metrics.completeness * 100).toFixed(0) }}%
          </el-descriptions-item>
          <el-descriptions-item label="数据来源">
            {{ result.data_sources.join(', ') }}
          </el-descriptions-item>
        </el-descriptions>

        <el-divider />

        <!-- 报告正文 -->
        <div class="markdown-body" v-html="renderMarkdown(result.report_text)" />

        <!-- 错误信息（如有） -->
        <el-alert
          v-for="(error, index) in result.errors"
          :key="index"
          :title="error"
          type="warning"
          :closable="false"
          style="margin-top: 10px"
        />
      </el-card>

      <el-button @click="reset" style="margin-top: 20px">
        重新生成
      </el-button>
    </div>
  </div>
</template>

<script setup>
import { ref, reactive } from 'vue'
import axios from 'axios'
import MarkdownIt from 'markdown-it'

const md = new MarkdownIt()
const currentStep = ref(0)
const progress = ref(0)
const progressText = ref('')
const loading = ref(false)
const result = ref(null)
const formRef = ref()

const form = reactive({
  disaster_overview: {
    disaster_type: '',
    disaster_name: '',
    occurrence_time: '',
    end_time: '',
    location: '',
    disaster_scale: '',
    final_deaths: 0,
    final_injured: 0,
    affected_population: 0
  },
  response_activation: {
    response_level: '',
    activation_time: '',
    command_center: '',
    leading_unit: '',
    participating_departments: []
  },
  rescue_statistics: {
    total_rescued: 0,
    rescued_alive: 0,
    evacuated_people: 0,
    resettled_people: 0,
    treated_patients: 0
  },
  resource_utilization: {
    total_personnel: 0
  }
})

const nextStep = async () => {
  currentStep.value = 1
  progress.value = 10
  progressText.value = '正在提交数据...'

  try {
    // 模拟进度
    const progressTimer = setInterval(() => {
      if (progress.value < 90) {
        progress.value += 5
        if (progress.value < 30) {
          progressText.value = '正在分析灾情数据...'
        } else if (progress.value < 60) {
          progressText.value = '正在检索历史案例...'
        } else {
          progressText.value = '正在生成评估报告...'
        }
      }
    }, 2000)

    const response = await axios.post(
      'http://localhost:8008/reports/post-rescue-assessment',
      form,
      { timeout: 180000 }
    )

    clearInterval(progressTimer)
    progress.value = 100
    progressText.value = '报告生成成功！'

    setTimeout(() => {
      result.value = response.data
      currentStep.value = 2
    }, 1000)

  } catch (error) {
    console.error(error)
    ElMessage.error(error.response?.data?.detail || '生成失败')
    currentStep.value = 0
  }
}

const renderMarkdown = (text) => {
  return md.render(text)
}

const getConfidenceType = (score) => {
  if (score >= 0.8) return 'success'
  if (score >= 0.6) return 'warning'
  return 'danger'
}

const downloadReport = () => {
  const blob = new Blob([result.value.report_text], { type: 'text/markdown' })
  const url = URL.createObjectURL(blob)
  const a = document.createElement('a')
  a.href = url
  a.download = `${form.disaster_overview.disaster_name}_评估报告.md`
  a.click()
  URL.revokeObjectURL(url)
}

const reset = () => {
  currentStep.value = 0
  progress.value = 0
  result.value = null
}
</script>

<style scoped>
.generating {
  text-align: center;
  padding: 100px 0;
}

.markdown-body {
  padding: 20px;
  background: #f9f9f9;
  border-radius: 4px;
}
</style>
```

---

## 7. 错误码说明

| 状态码 | 说明 | 常见原因 | 解决方案 |
|--------|------|---------|---------|
| 200 | 成功 | - | - |
| 400 | 请求参数错误 | 缺少必填字段 | 检查必填字段 |
| 422 | 数据验证失败 | 日期格式错误、数字为负数 | 检查数据类型和格式 |
| 500 | 服务器内部错误 | LLM调用失败 | 稍后重试或联系管理员 |
| 502 | Bad Gateway | LLM返回空 | 检查输入数据质量 |
| 504 | Gateway Timeout | 超过180秒 | 简化输入或增加超时时间 |

---

## 8. 性能指标

### 8.1 响应时间

| 输入复杂度 | 预期响应时间 | 建议超时 |
|-----------|------------|---------|
| 最小输入（4个必填模块） | 60-80秒 | 120秒 |
| 完整输入（8个模块全填） | 100-140秒 | 180秒 |
| 超大输入（多个力量+详细时间线） | 120-160秒 | 200秒 |

### 8.2 报告质量

| 数据完整度 | 报告长度 | 置信度 | 适用场景 |
|-----------|---------|--------|---------|
| 25%（仅必填） | 4000-5000字 | 0.2-0.4 | 快速了解 |
| 50%（必填+部分可选） | 5000-6000字 | 0.4-0.6 | 内部参考 |
| 75%以上（大部分字段） | 6000-7000字 | 0.6-0.9 | 正式报告 |

---

## 9. 注意事项

### 9.1 数据质量建议

1. **时间精确性**: 所有时间字段使用ISO 8601格式（YYYY-MM-DDTHH:mm:ss）
2. **数据一致性**: 确保统计数字前后一致（如救援人数 ≤ 受影响人口）
3. **描述详细性**: 可选字段尽量填写，提升报告质量和置信度

### 9.2 最佳实践

1. **分步填写**: 建议前端设计分步表单，降低填写难度
2. **数据校验**: 前端进行基础校验（日期逻辑、数字范围）
3. **进度提示**: 显示"预计需要2分钟"的提示，避免用户焦虑
4. **结果导出**: 提供Markdown/PDF/Word导出功能

### 9.3 安全建议

1. **敏感信息脱敏**: 涉及具体人员姓名、电话等信息应脱敏
2. **权限控制**: 评估报告属于敏感文档，需严格权限控制
3. **审计日志**: 记录谁在何时生成了哪个报告

---

## 10. 测试数据

完整测试数据文件：
- 最小输入：`tests/fixtures/post_rescue_assessment_minimal_input.json`
- 完整输入：`tests/fixtures/post_rescue_assessment_complete_input.json`

详细测试报告：`docs/POST_RESCUE_ASSESSMENT_API_测试报告.md`

---

## 11. 常见问题（FAQ）

### Q1: 与"救灾方案上报"接口有什么区别？
**A**:
| 对比项 | 救灾方案上报 | 救援评估报告 |
|--------|------------|------------|
| 时机 | 灾害发生时（紧急） | 救援结束后（总结） |
| 输入 | 简单描述 | 完整过程数据 |
| 输出 | 行动方案 | 评估报告 |
| 时效 | 30-60秒 | 60-140秒 |

### Q2: 最少需要填哪些字段？
**A**: 4个必填模块：
- disaster_overview（9个字段）
- response_activation（5个字段）
- rescue_statistics（5个字段）
- resource_utilization（至少1个字段）

### Q3: 如何提高置信度？
**A**:
1. 填写更多可选字段（timeline、forces_deployed等）
2. 提供详细的文字描述
3. 确保数据前后一致

### Q4: 报告可以二次编辑吗？
**A**:
- 接口返回Markdown文本，可以在前端或编辑器中修改
- 建议提供"编辑"功能，允许专家审核修改后再发布

### Q5: 支持批量生成吗？
**A**:
- 接口设计为单次生成
- 如需批量，建议前端实现队列机制，逐个调用

---

## 12. 更新日志

| 版本 | 日期 | 更新内容 |
|------|------|---------|
| v1.0 | 2025-11-02 | 初始版本，支持7章评估报告生成 |

---

## 13. 联系支持

- **技术支持**: tech-support@emergency.cn
- **问题反馈**: [GitHub Issues](https://github.com/your-repo/issues)
- **文档更新**: 每周五更新

---

**文档维护**: AI应急大脑团队
**最后审核**: 2025-11-02
